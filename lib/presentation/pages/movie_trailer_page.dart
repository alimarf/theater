part of "pages.dart";

class MovieTrailerPage extends StatelessWidget {
  const MovieTrailerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MovieTrailerComponent();
  }
}
